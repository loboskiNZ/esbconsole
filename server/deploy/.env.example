# Copy to server/deploy/.env (gitignored). Never commit secrets.
#
# Forge → Sites → band.edandtheshadowboys.com → Deployments → Deploy hook
# FORGE_DEPLOY_HOOK_URL=https://forge.laravel.com/servers/.../sites/.../deploy/http?token=...
